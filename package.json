{
  "name": "dialedin-nuvei-integration",
  "version": "1.0.0",
  "description": "Payment tokenization integration between DialedIn and Nuvei",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node test/test-integration.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "axios": "^1.4.0",
    "dotenv": "^16.0.3",
    "express-rate-limit": "^6.7.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
{
  "name": "dialedin-nuvei-integration",
  "version": "1.0.0",
  "description": "Payment tokenization integration between DialedIn and Nuvei",
  "main": "server.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
